import React from "react";
import ReactDOM from "react-dom/client";
import App from "./mapping-components/App";
import "./mapping-components/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
